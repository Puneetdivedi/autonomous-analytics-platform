# Slim dependency set for the free Vercel serverless deployment (see api/index.py).
# Omits scipy/sklearn/statsmodels (unused), plus redis/qdrant/langfuse/asyncpg and
# the LLM provider SDKs (all lazy-loaded and unused in keyless stub mode) to stay
# under the serverless size limit. Full deps live in pyproject.toml.
fastapi>=0.111
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20
asyncpg>=0.29
python-jose[cryptography]>=3.3
bcrypt>=4.1
python-multipart>=0.0.9
email-validator>=2.1
langchain-core>=0.2
langgraph>=0.1
pandas>=2.2
numpy>=1.26
matplotlib>=3.9
reportlab>=4.2
openpyxl>=3.1
python-docx>=1.1
sqlparse>=0.5
structlog>=24.1
tenacity>=8.3
httpx>=0.27
